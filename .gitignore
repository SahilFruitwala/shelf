node_modules
.DS_Store
dist
dist-ssr
*.local
.env
.nitro
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json
dev.db
.vercel/
.pnpm-store
# Personal Trakt export dumps at the repo root. Anchored with a leading slash
# so it can't also swallow scripts/trakt-import/, which is tracked.
/trakt*
scripts/trakt-import/output


*.pyc
*.py